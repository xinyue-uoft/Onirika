#!/usr/bin/env bash
# Onirika Web Launcher — wrapper that uses python -m to avoid .pth issues.
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
PROJECT_DIR="$(dirname "$SCRIPT_DIR")"
cd "$PROJECT_DIR"
exec uv run python -m onirika.web "$@"
